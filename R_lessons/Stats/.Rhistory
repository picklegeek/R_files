head(mtcars)
library(ggplot2)
ggplot(mtcars,aes(x=mpg,y=wt))+geom_point()
mtcars %>%
cor(mpg,wt)
cor(mtcars$mpg,mtcars$wt)
group_by(mtcars$gear)
group_by(gear)
library(dplyr)
group_by(gear)
group_by(mtcars$gear)
group_by(mtcars$gear)%>%
cor(mtcars$mpg,mtcars$wt)
mtcars %>% gropp_by(gear) %>% summarize(N=n(), r = cor(mpg,wt))
mtcars %>% group_by(gear) %>% summarize(N=n(), r = cor(mpg,wt))
ggplot(mtcars,aes(mpg,wt,color = factor(gear))) + geom_point()
library(rmarkdown)
render("Correlation.Rmarkdown",output_file = "correlation.html")
